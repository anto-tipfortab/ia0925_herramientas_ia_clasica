[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
filterwarnings = [
    # google-api-core emits a Python-version FutureWarning that is noise here.
    "ignore::FutureWarning",
]
